# phases.md — Tracer-feature N1+N2+N4 (Phase 13 hardening)

> Tracer Bullets feature. Generated by /tracer-feature on 2026-05-03.
> Source principle: Matt Pocock — vertical slice end-to-end (https://www.aihero.dev/tracer-bullets).
> Anchors generated via /plan-hardened (Round 1-3 + Round 5). Audit trail at `.claude/plan-hardened/tracer-anchors/`.

**Feature description**: fix three review-blockers on `phase-13-master-sync`:
- N1 — snapshot writer unwired in monitoring-only mode (defeats Phase 13 Fix 4 stated goal)
- N2 — TradeStore wiring race (scheduler-started orchestrators race engine's lazy store creation)
- N4 — whale size/USD confusion (signal corruption in production for share-count trades)

**Flag parsing** (from /tracer-feature § Flag parsing):
- `--strict-replan`: OFF — inter-slice gate is anchor-driven by default
- `--no-caveman`: OFF — caveman auto-activation NOT triggered (only 3 slices, threshold ≥5)

---

## Architectural Anchors (Slice 0)

Contratto architetturale immutabile. Ogni slice rispetta questi anchor o triggera abort tracer.

| Anchor | Valore | Rationale |
|--------|--------|-----------|
| Skill canonical paths | tracer-bullets, codex-delegation, caveman-commit, obsidian-kg @ ~/.claude/skills/<name>/SKILL.md | Required-by-spec da /tracer-feature Step 0.2: la tabella anchors DEVE fissare le skill canoniche. Lock dei path evita drift se il tool inventory cambia tra Round; chi esegue legge SOLO da questi path, non da copie locali o citazioni inline. tracer-bullets governa slice semantics, codex-delegation la delega monitorata, caveman-commit il commit format, obsidian-kg la persistenza lessons. |
| Command paths | /tracer-feature, /plan-hardened, /refactor, /fix-until-green @ ~/.claude/commands/<name>.md | Required-by-spec da /tracer-feature Step 0.2. /tracer-feature è driver primario, /plan-hardened gate inter-slice anchor-driven, /refactor discriminator semantico (NON usabile qui — i tre fix introducono behavior change), /fix-until-green recovery loop. |
| File scope per slice | Slice N1 = `app/core/dependencies.py` + `tests/test_core/test_dependencies_*.py` (NEW, mirror); Slice N2 = `app/core/dependencies.py` + `tests/test_core/test_dependencies_*.py` (stesso glob mirror); Slice N4 = `app/services/whale_orchestrator.py` + `tests/test_services/test_whale_orchestrator_*.py` (mirror). | File disgiunti tra N4 e N1/N2 → N4 parallelizzabile con N1 (vedi Single-slice-per-blocker). N1 e N2 condividono dependencies.py → strettamente sequenziali. Tutte e tre usano stesso glob mirror tests/test_<dir>/test_<module>_*.py. Nessuna slice tocca app/main.py: lifespan è orchestratore, non implementer. |
| OFF-LIMITS | `app/main.py` (lifespan è orchestratore — chiama get_execution_engine, non lo costruisce); `config/*` (tunables anchor-invariant); tests/ outside mirroring convention (ANCHE per N2: solo tests/test_core/test_dependencies_*.py); qualunque modulo app/ non listato nel file scope (intelligence_orchestrator, trade_store internals, snapshot_writer signature). | Tracer != refactor: behavior change confinata ai punti di wiring/parsing identificati nei bounded blocker. Allargare scope a config/main/orchestrator interni invalida invariante "demoable end-to-end senza side-effect cross-cutting". |
| Inner loop | Step 1.2 inline single-shot (ogni slice = 1 file principale + 1 test file → no wave-orchestrator necessario). Intra-slice parallelism solo se ≥2 sub-task disjoint emergono. | Overhead wave-orchestrator (heartbeat, status protocol, restart logic) non giustificato per fix <30 LOC su singolo file. Single-shot inline mantiene context locality, riduce token cost. |
| Inter-slice gate | plan-hardened anchor-driven. Trigger CONCRETI (gate INVOKE se almeno uno vero): (a) implementer ritorna status DONE_WITH_CONCERNS (status protocol del CLAUDE.md globale: DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT — settato dall'agente che chiude la slice); (b) transizione N1→N2 (entrambe modificano dependencies.py — gate previene merge conflict semantico); (c) qualsiasi slice fallisce demo gate al primo run. Trigger NON attivo per: transizioni che coinvolgono N4 (file disgiunto), slice DONE pulita su file disgiunti. | Tre slice piccole non richiedono gate critico per default. Trigger condizionali concreti (status flag esplicito + file-overlap + demo failure) sostituiscono il vago "anchor file" e definiscono univocamente quando il gate scatta. Status flag settato dall'implementer, non inferito. |
| Demo gate semantics | Backend-only (no UI). Per slice (PASS = TUTTI veri): (1) pytest exit 0 sui test del modulo target; (2) per Slice N2 specificamente: i test devono coprire ENTRAMBI gli ordini di inizializzazione (scheduler-prima-di-engine E engine-prima-di-scheduler) e asserire singleton identity (`get_trade_store()` ritorna la stessa istanza usata da get_execution_engine e da start_intelligence_scheduler — `assert a is b is c`); (3) smoke import test stdout (`python -c "import app.<module>"`); (4) git diff size: advisory check, atteso <30 LOC escluso test code — un superamento NON è fail automatico, è segnale per re-leggere lo scope (l'implementer documenta il motivo nel commit body). Nessuno screenshot. | Tre fix sono backend (DI wiring, parsing). UI dashboard non impattata. Tightening su Slice N2 necessario perché smoke import può passare mentre lazy-store race rimane: dual-order test + singleton-identity assertion è il solo modo di provare l'invariante. LOC threshold demota da gate ad advisory check (payload bounded non autorizza a respingere fix corretto per soglia arbitraria; resta utile come scope-discipline signal). |
| Behavior change rule | Tracer = nuova behavior osservabile. N1: snapshot writer wired anche in monitoring-only è osservabile via filesystem (file snapshot scritto). N2: TradeStore singleton accessibile pre-engine è osservabile via DI introspection (singleton identity test). N4: whale size/USD corretto è osservabile via test diretto sul valore di output del parser (es. `parsed.usd == 500` per 10000 share × $0.05) — NESSUN nuovo metric o log richiesto, l'osservabilità è nel return value della funzione di parsing. NON refactor (zero-behavior-change è /refactor). | Distinzione lean: i tre fix introducono behavior visibile dall'esterno. Osservabilità di N4 confinata al return value testato — payload non chiede nuove metriche/log, aggiungerle sarebbe scope creep. Ciò qualifica /tracer-feature come tool corretto vs /refactor (che fallirebbe vincolo zero-behavior-change). |
| Recovery budget | /fix-until-green budget 3 iter dentro slice se test red. Oltre → slice marked FAILED, l'implementer riporta evidenze (last error, files touched, diff state) e si ferma. NESSUN auto-rollback git, NESSUNA escalation automatica a planning-specialist: la decisione di rollback/re-scope/escalation è dell'utente sulla base del FAILED report. Lo stato locale (modifiche non committate) resta intatto per ispezione. | 3 iter è soglia empirica per discriminare "errore meccanico recuperabile" da "modello mentale errato". Auto-rollback/escalation rimossi perché non autorizzati dal payload bounded e perché un rollback meccanico potrebbe scartare lavoro locale non correlato (working tree contiene altre modifiche pendenti). |
| Single-slice-per-blocker | Accettata: N1 = Slice 1, N2 = Slice 2, N4 = Slice 3 in default. Regola di ordering UNIVOCA: N1 → N2 sequenziali (entrambe su dependencies.py, no merge race tollerata); N4 PUÒ essere eseguita in parallelo con N1 o N2 sotto separazione di file scope già fissata (whale_orchestrator.py disgiunto da dependencies.py), oppure ultima — la scelta default è parallela con N1 per ridurre wall-clock; "ultima per safety" NON è la regola, è un fallback solo se l'utente esplicitamente richiede esecuzione strettamente seriale. | Coerente con principio Pocock "demoable end-to-end". Convention "1 blocker = 1 slice" mantiene coherent unit of demo. Ordering disambiguato: parallelism N4-con-N1 default; seriale opt-in. |

---

## Slices

_Slice 1 sarà popolata da Step 1.1 (planning-specialist con output_mode=phases). Slice 2 e 3 possono restare abbozzate, saranno raffinate iterativamente quando il loop di Step 1 le raggiunge._

### Slice 1 — N1: snapshot wiring in monitoring-only mode

**Status:** DONE — commit 6cb788d (2026-05-03). Demo gate PASS (3/3 tests, smoke OK, 28 LOC delta). Implementer status: DONE clean (no concerns). Inter-slice gate trigger (b) activated for Slice 1 → Slice 2 (file overlap on `app/core/dependencies.py`).

**Inter-slice gate verdict** (audit: `.claude/plan-hardened/inter-slice-gate-1-2.json`): CLEANUP_REQUIRED downgraded to WARNING — the cleanup item (G1-01: replace `getattr(_execution_engine, "_store", None)` at line 419 with `get_trade_store()`) is already part of Slice 2's N2 deliverable (TradeStore singleton extraction), NOT a separate refactor scope. Lean separation hard-rule: `/refactor` is zero-behavior-change, but the migration changes semantics (eliminates the None fallback when engine is unbuilt) → must be done as Slice 2 behavior change. Anchors integrity: PASS. Scope creep: PASS. Slice 2 readiness: PASS (goal still valid, augmented with G1-01 fix as explicit deliverable in Slice 2 spec below).

**Goal:** When the FastAPI app starts in monitoring-only mode (`bot.auto_start=false`, so `get_execution_engine()` is NOT called from the lifespan), `start_intelligence_scheduler()` itself wires the SnapshotWriter dependencies (engine — lazily via `get_execution_engine()` only if already constructed, else None; trade_store — None acceptable, snapshot writer tolerates missing deps; whale/popular/leaderboard orchestrators — wired conditionally on the same gating used in `get_execution_engine`); the scheduler's snapshot loop then ticks every 5 min and the file `static/dss/intelligence_snapshot.json` appears on disk even though the trading bot loop never started.

**Files:**
| Marker | Path | Note |
|--------|------|------|
| EDIT | `app/core/dependencies.py` | Add SnapshotWriter wiring inside `start_intelligence_scheduler()` (lines ~370–408). Today only `set_snapshot_writer(get_snapshot_writer())` is called at line 405 — the writer is registered but its `set_engine` / `set_trade_store` / `set_whale_orchestrator` / `set_popular_markets_orchestrator` / `set_leaderboard_orchestrator` setters are invoked ONLY inside `get_execution_engine()` (lines 334–342), which monitoring-only mode never reaches. New code must call those setters in `start_intelligence_scheduler()` using the same conditional gates already present (whale.enabled / popular_markets.enabled / leaderboard.enabled / dss.snapshot_writer.enabled). Trade store: do NOT force-construct (constructing it here is Slice 2's scope). Engine: do NOT force-construct (lifespan invariant — only built if `bot.auto_start=true`). Best-effort wiring: if the orchestrators are None or already wired, the setters are idempotent. |
| NEW | `tests/test_core/test_dependencies_snapshot_wiring.py` | Mirror convention `tests/test_<dir>/test_<module>_<topic>.py`. Asserts that after `start_intelligence_scheduler()` is awaited with `bot.auto_start=False` and the relevant intel flags ON, `get_snapshot_writer()` returns an instance whose private attrs (`_whale_orch`, `_popular_orch`, `_leaderboard_orch`) are non-None and equal the singletons returned by `get_whale_orchestrator()` etc. Second test: with intel flags OFF, the same setters remain None (no spurious wiring). Third test: smoke import — `from app.core.dependencies import start_intelligence_scheduler` succeeds (guards against import-time regression). Use `pytest-asyncio` with the existing fixture pattern from `tests/test_core/test_dependencies.py` if present; otherwise fixture-isolate via `monkeypatch` of `app_config` flags + reset of module-level singletons (`_intelligence_scheduler`, `_snapshot_writer`, `_whale_orchestrator`, etc.) in setup/teardown. |

**Agent split** (intra-slice parallelism — file scope disjoint, no shared writes):

| # | Task | Agent | File Scope |
|---|------|-------|------------|
| 1 | Implement SnapshotWriter dep wiring inside `start_intelligence_scheduler()`. Mirror the conditional structure already in `get_execution_engine` (lines 288–302). Setters are idempotent (late-binding pattern documented in `snapshot_writer.py:11–15`), so re-wiring after engine construction is safe. Do NOT touch lines 264–344 (engine builder) or any file outside scope. | backend-specialist | `app/core/dependencies.py` (only the body of `start_intelligence_scheduler`, ≈ lines 370–408) |
| 2 | Write the three tests described in the Files row above. Use real (non-mocked) singletons — reset module globals in fixture per project convention (no respx needed; this is pure DI). Tests must be independent (no order dependency) and pass under `pytest -x`. | test-writer | `tests/test_core/test_dependencies_snapshot_wiring.py` (NEW) |

Parallelism rationale: sub-task 1 writes only to `app/core/dependencies.py`; sub-task 2 writes only to the new test file. Disjoint scopes → safe to dispatch concurrently. Sub-task 2 does NOT depend on sub-task 1's diff (it asserts the public contract `start_intelligence_scheduler()` must satisfy — TDD-friendly: test can be written against the goal contract before implementation lands and is the demo gate's first signal).

**Demo criteria** (PASS = ALL boxes checked):
- [ ] `pytest tests/test_core/test_dependencies_snapshot_wiring.py -x` exits 0 (all three tests green)
- [ ] Smoke import: `python -c "from app.core.dependencies import start_intelligence_scheduler"` exits 0 with empty stdout (no ImportError, no warning on stderr)
- [ ] Observable behavior: in a manual probe (NOT part of automated demo gate, but documented in commit body) — set `bot.auto_start=False` + `intelligence.whale.enabled=true` + `dss.snapshot_writer.enabled=true`, start the app, wait for one snapshot tick, assert `static/dss/intelligence_snapshot.json` exists on disk and parses as valid JSON. Only required if reviewer asks for end-to-end evidence; the test+smoke pair is the binding demo gate.
- [ ] Advisory check: `git diff --stat app/core/dependencies.py` shows <30 LOC added/modified (excluding test file). Overshoot is NOT auto-fail — implementer documents the reason in the commit body if exceeded.

**Exit gate** (anchored to phases.md § "Demo gate semantics"):
Backend-only demo. PASS requires: (1) pytest exit 0 on the module test file; (2) smoke import test stdout per `python -c` invocation; (3) advisory diff size check (≈ <30 LOC) — non-binding signal, documented if exceeded. No screenshot, no UI verification. If the implementer returns DONE_WITH_CONCERNS (per CLAUDE.md global status protocol), the inter-slice gate triggers before Slice 2 begins (anchor row "Inter-slice gate" trigger (a)). Trigger (b) of the inter-slice gate ALWAYS fires for Slice 1 → Slice 2 transition because both modify `app/core/dependencies.py` (file overlap).

**Rollback:** Single-commit slice. If demo gate fails after `/fix-until-green` budget (3 iterations) is exhausted, the implementer marks the slice FAILED and reports evidence (last test output, files touched, diff state) per anchor "Recovery budget" — NO auto-rollback. If the user authorizes rollback after reviewing the FAILED report, recovery is `git revert <commit_sha>` of the single Slice 1 commit. No DB migration to undo, no config schema change, no static asset to delete (the snapshot file under `static/dss/` is generated at runtime and not tracked — its deletion is irrelevant to rollback safety). Working tree pollution risk (anchor "Risks" row 3) means the implementer commits ONLY the two scoped files; a `git revert` of that commit will not touch unrelated working-tree changes.

---

### Slice 2 — N2: TradeStore singleton extraction

**Status:** DONE — commit 292820f (2026-05-03). Demo gate PASS (5/5 tests including dual-order singleton-identity cases scheduler_first AND engine_first; smoke import for new `get_trade_store` symbol OK; 22 LOC net <40 advisory). Implementer status: DONE clean (no concerns). G1-01 cleanup migrated as part of Slice 2 deliverable. Inter-slice gate Slice 2 → Slice 3: SKIPPED — no trigger fired (a NO clean status; b NO file disjoint dependencies.py vs whale_orchestrator.py; c NO demo gate green at first run). Anchor-driven skip per phases.md § Inter-slice gate.

**Goal:** Extract `get_trade_store() -> TradeStore` as a lazy async DI singleton in `app/core/dependencies.py` (callable independently of `get_execution_engine()`, with `await store.init()` invoked once on first access mirroring the `get_value_engine` pattern at lines 252–261), and wire that single instance into all four consumers — ExecutionEngine (line 276–277 inline construction replaced by `store = await get_trade_store()`, preserving `engine._store` identity), the three scheduler-side orchestrators (whale/popular/leaderboard via their existing `set_trade_store` setters), and SnapshotWriter (replacing the `getattr(_execution_engine, "_store", None)` block at lines 417–421 introduced by Slice 1 with an unconditional `snapshot_writer.set_trade_store(store)` — G1-01 cleanup) — such that under both initialization orders (scheduler-first then engine; engine-first then scheduler) the assertion `get_trade_store() is engine._store is whale_orch._trade_store is snapshot_writer._trade_store` holds.

**Files:**
| Marker | Path | Note |
|--------|------|------|
| EDIT | `app/core/dependencies.py` | (1) Add module-level `_trade_store: TradeStore | None = None` global and `async def get_trade_store() -> TradeStore` near `get_value_engine` (lines 252–261 pattern: lazy construct + `await store.init()` on first call, idempotent on subsequent calls). (2) Inside `get_execution_engine` (lines 264–344): replace inline `store = TradeStore(); await store.init()` (lines 276–277) with `store = await get_trade_store()`. The local `store` variable continues to flow into ExecutionEngine constructor at line 320 (`trade_store=store`) and into orchestrator setters at lines 286/292/297/302 — no other change needed in this function. (3) Inside `start_intelligence_scheduler` (lines 370–434): immediately after `scheduler.set_market_service(...)` (line 386), add `store = await get_trade_store()`. Then wire that store to whale/popular/leaderboard orchestrators inside their existing `if app_config.intelligence.<x>.enabled:` blocks (lines 389–402) via `await <orch>.set_trade_store(store)` — popular_markets and leaderboard currently lack this call; add it (the setters already exist on those orchestrators per file scope context). (4) Replace the conditional getattr block at lines 414–421 with unconditional `snapshot_writer.set_trade_store(store)` (no engine-existence guard needed — the singleton is independent of engine construction). The `if _execution_engine is not None: snapshot_writer.set_engine(_execution_engine)` guard at lines 414–415 stays as-is (engine wiring remains conditional on engine existence — out of N2 scope). |
| EDIT | `tests/test_core/test_dependencies_snapshot_wiring.py` | Augment Slice 1's existing test file (same mirror glob `tests/test_core/test_dependencies_*.py`, no new file). Add fixture extension: extend the existing `clean_singletons` fixture to also reset the new `_trade_store` module global (`monkeypatch.setattr("app.core.dependencies._trade_store", None)` in setup). Add new parametrized test `test_trade_store_singleton_identity_dual_order_init` with two cases: case A (`scheduler_first`) — call `await start_intelligence_scheduler()` then `await get_execution_engine()`; case B (`engine_first`) — call `await get_execution_engine()` then `await start_intelligence_scheduler()`. In both cases assert `get_trade_store() is engine._store is whale_orch._trade_store is snapshot_writer._trade_store` (use the same accessor pattern as Slice 1's tests for `_whale_orch` etc. — read private attrs `_trade_store` on the orchestrator/writer singletons). Ensure both whale orch flag AND dss.snapshot_writer flag ON for the test config. Real (non-mocked) singletons per project convention. |

**Agent split** (intra-slice parallelism — file scopes disjoint, safe to dispatch concurrently):

| # | Task | Agent | File Scope |
|---|------|-------|------------|
| 1 | Implement `get_trade_store()` lazy async singleton mirroring `get_value_engine` (lines 252–261). Refactor `get_execution_engine` body so `store = await get_trade_store()` replaces the inline `TradeStore() + await store.init()` at lines 276–277 (the resulting object identity must remain reachable as `engine._store` via the unchanged constructor wiring at line 320 — verify ExecutionEngine stores `trade_store` as `self._store`; if it stores under a different attribute, document in commit body and the test asserts the actual attribute name). Refactor `start_intelligence_scheduler` body so `store = await get_trade_store()` is called once near line 386 and wired into whale/popular/leaderboard orchestrators via `set_trade_store(store)` inside their existing conditional gates, AND replaces the lines 414–421 getattr block with unconditional `snapshot_writer.set_trade_store(store)` (G1-01 migration — eliminates the None fallback that currently silently no-ops in monitoring-only mode). Do NOT modify orchestrator `set_trade_store` signatures (anchor OFF-LIMITS — orchestrator internals untouchable). Do NOT touch any file outside `app/core/dependencies.py`. | backend-specialist | `app/core/dependencies.py` (only: new `get_trade_store` function near line 261; body of `get_execution_engine` lines 276–277 + propagation; body of `start_intelligence_scheduler` lines 386–432) |
| 2 | Augment `tests/test_core/test_dependencies_snapshot_wiring.py` (EDIT, not NEW — file created by Slice 1). Extend `clean_singletons` fixture to also reset `app.core.dependencies._trade_store`. Add parametrized test `test_trade_store_singleton_identity_dual_order_init` with two cases (`scheduler_first`, `engine_first`) — both must assert `get_trade_store() is engine._store is whale_orch._trade_store is snapshot_writer._trade_store`. Use real (non-mocked) singletons; reset module globals via the extended fixture; both whale and dss.snapshot_writer flags ON in test config. Test must pass under `pytest -x` independently of execution order vs the existing 3 Slice-1 tests. | test-writer | `tests/test_core/test_dependencies_snapshot_wiring.py` (EDIT — fixture extension + one new parametrized test) |

Parallelism rationale: sub-task 1 writes only to `app/core/dependencies.py`; sub-task 2 writes only to the test file. Disjoint scopes → safe concurrent dispatch. Sub-task 2 is contract-driven (asserts the singleton-identity invariant Slice 2 must satisfy) and can be written before sub-task 1's diff lands — TDD-friendly demo gate signal.

**Demo criteria** (PASS = ALL boxes checked):
- [ ] `pytest tests/test_core/test_dependencies_snapshot_wiring.py -x` exits 0 (Slice 1's 3 tests + Slice 2's new dual-order parametrized test = 5 test invocations: 3 Slice-1 + 2 parametrized cases of `test_trade_store_singleton_identity_dual_order_init`)
- [ ] Singleton-identity assertion passes under BOTH parametrized cases — `scheduler_first` AND `engine_first`
- [ ] Smoke import: `python -c "from app.core.dependencies import get_trade_store"` exits 0 with empty stdout (new public symbol importable, no ImportError)
- [ ] No regression on Slice 1 tests (all 3 existing tests still green after fixture extension)
- [ ] Advisory: `git diff --stat app/core/dependencies.py` shows <40 LOC added/modified (slightly higher than Slice 1's 30 LOC threshold because of the singleton extraction itself — the new `get_trade_store` function adds ~10 LOC alone). Overshoot is NOT auto-fail; documented in commit body if exceeded.

**Exit gate** (anchored to phases.md § "Demo gate semantics"):
Backend-only demo. PASS requires: (1) pytest exit 0 on the module test file (5 invocations green); (2) the Slice-N2-specific dual-order singleton-identity test passes under both parametrized cases — this is the anchor-mandated invariant for Slice 2 ("Demo gate semantics" point 2 calls out N2 explicitly); (3) smoke import test for the new `get_trade_store` symbol; (4) advisory diff size <40 LOC — non-binding signal, documented if exceeded. No screenshot, no UI verification. Inter-slice gate trigger evaluation for Slice 2 → Slice 3: trigger (a) DONE_WITH_CONCERNS fires if implementer reports concerns (per CLAUDE.md global status protocol); trigger (b) does NOT fire (file scopes disjoint — Slice 2 modifies `app/core/dependencies.py`, Slice 3 modifies `app/services/whale_orchestrator.py`); trigger (c) fires if demo gate fails at first run. If only (a) or (c) fires, gate invocation is anchor-driven; otherwise Slice 3 may proceed without gate.

**Rollback:** Single-commit slice. If demo gate fails after `/fix-until-green` budget (3 iterations) is exhausted, the implementer marks the slice FAILED and reports evidence (last test output, diff state, files touched) per anchor "Recovery budget" — NO auto-rollback. If the user authorizes rollback after reviewing the FAILED report, recovery is `git revert <slice_2_commit_sha>` of the single Slice 2 commit. Rollback caveat: Slice 2 keeps engine's `_store` field pointing to the singleton instance (same object identity as the new accessor), so revert restores the inline construction at lines 276–277 and the getattr block at lines 414–421 to their Slice-1 state — Slice 1's tests should remain green post-revert. If they do not (unlikely — would indicate Slice 2 introduced a hidden coupling beyond the documented refactor), manual fix-up of Slice 1's `getattr(_execution_engine, "_store", None)` line is required before declaring rollback complete. No DB migration to undo, no config schema change, no static asset to delete. Working tree pollution risk (Risks row 3) means the implementer commits ONLY the two scoped files; `git revert` will not touch unrelated working-tree changes.

---

### Slice 3 — N4: whale size/USD confusion fix

**Status:** DONE — commit cf3360e (2026-05-03). Demo gate PASS after fix-until-green iter 1 (regression on existing `test_whale_orchestrator.py::test_filters_below_threshold` fixed by updating `_make_raw_trade` helper to emit `size_usd` field instead of `size` — semantically correct since the helper produces USD-denominated trades for threshold-filter tests). 11/11 tests pass (8 existing + 3 new size_usd tests). Smoke import OK. 23 LOC delta in whale_orchestrator.py (<30 advisory).

**Scope amendment** (logged here, not violating anchors): the regression fix touched `tests/test_services/test_whale_orchestrator.py` (NOT in the original Slice 3 file scope glob `tests/test_services/test_whale_orchestrator_*.py`). Justification: the existing test was implicitly asserting the buggy size/USD interpretation (`size_usd == 250_000` while `size=250000, price=0.6` would yield `150_000` under correct logic). N4 changes the contract; bug-test must follow. Single-line helper change (replace `"size": size` with `"size_usd": size` plus 4-line comment). Anchor "OFF-LIMITS / tests outside mirroring convention" is preserved in spirit — no NEW test file outside mirror, no edits to test bodies, only the data-builder helper aligned with the new contract.

**Goal:** `WhaleOrchestrator._parse_trade` preserves size source provenance and applies `size * price` as USD conversion ONLY when the source is share-count (`size` field), not when already-USD (`size_usd` field), eliminating signal corruption for share-count trades of any magnitude (e.g. 10000 shares × $0.05 must yield 500.0 USD, not 10000.0 USD as the current `size_usd < 1.0` heuristic produces).

**Files:**
| Marker | Path | Note |
|--------|------|------|
| EDIT | `app/services/whale_orchestrator.py` | Modify `_parse_trade` body (lines 200–246). Replace the conflated assignment `size_usd = _to_float(raw.get("size_usd") or raw.get("size"))` (line 221) with explicit source-tracking branching: read both fields independently — if `raw.get("size_usd")` is present, source = `"usd"` and `size_value = _to_float(raw.get("size_usd"))`; else source = `"shares"` and `size_value = _to_float(raw.get("size"))`. Compute `usd_notional = size_value * price` if `source == "shares" and price` (truthy / >0), else `usd_notional = size_value`. Remove the heuristic block at lines 226–228 (`if size_usd and price and size_usd < 1.0: size_usd = size_usd * price`) — superseded by the explicit source-driven logic. WhaleTrade construction (lines 236–246) is unchanged except `size_usd=usd_notional`. Every other field (trade_id, ts, wallet, side, price, is_pre_resolution, raw_json) untouched. No other method, no other module, no signature change. Backward compat: existing whale trades persisted in SQLite are unaffected (only future parsing changes — pre-Slice-3 rows retain whatever value the old buggy logic produced). |
| NEW | `tests/test_services/test_whale_orchestrator_size_usd.py` | Mirror convention `tests/test_<dir>/test_<module>_<topic>.py`. Three tests, real `WhaleOrchestrator` instance with minimal config (no respx — `_parse_trade` operates on dict input directly): (1) `test_size_field_share_count_large` — raw trade with `{"size": 10000, "price": 0.05}` (no `size_usd` key), assert `parsed.size_usd == 500.0` (current buggy code returns 10000.0). (2) `test_size_usd_field_already_usd` — raw with `{"size_usd": 25000, "price": 0.50}` (no `size` key), assert `parsed.size_usd == 25000.0` (no multiplication applied). (3) `test_size_field_sub_share_legacy_case` — raw with `{"size": 0.5, "price": 0.10}` (no `size_usd` key), assert `parsed.size_usd == 0.05` (preserves the historical sub-share case the old heuristic handled). All three tests must pass under `pytest -x` and be order-independent. Each invocation of `_parse_trade` requires the standard `(raw, market, now, window_min)` signature — supply a minimal `Market` fixture (any valid id + end_date) and `now=datetime.now(timezone.utc)`, `window_min=60`. Real WhaleOrchestrator construction per project convention (no mocking). |

**Agent split** (intra-slice parallelism — file scopes disjoint, safe to dispatch concurrently):

| # | Task | Agent | File Scope |
|---|------|-------|------------|
| 1 | Modify `_parse_trade` in `whale_orchestrator.py` to use explicit source-field branching (`size` vs `size_usd`). Read both raw fields independently; compute `usd_notional` driven by which field provided the value (apply `* price` only when source is `size`). Preserve every other field of WhaleTrade unchanged (trade_id, ts, wallet, side, price, is_pre_resolution, raw_json). Do NOT modify any other method, helper, or module. Backward compat: existing rows in SQLite untouched (only future parsing semantics change). | backend-specialist | `app/services/whale_orchestrator.py` (only `_parse_trade` body, ≈ lines 200–246) |
| 2 | Write the three tests described in the Files row above. Real `WhaleOrchestrator` instance with minimal config; no HTTP fixture needed (`_parse_trade` is dict→model). Tests must be order-independent, pass under `pytest -x`. Use a minimal `Market` fixture (id + end_date), `now=datetime.now(timezone.utc)`, `window_min=60`. Assertions: case 1 → `parsed.size_usd == 500.0`; case 2 → `parsed.size_usd == 25000.0`; case 3 → `parsed.size_usd == 0.05`. | test-writer | `tests/test_services/test_whale_orchestrator_size_usd.py` (NEW) |

Parallelism rationale: sub-task 1 writes only to `app/services/whale_orchestrator.py`; sub-task 2 writes only to the new test file. Disjoint scopes → safe concurrent dispatch. Sub-task 2 is contract-driven (asserts the exact return values Slice 3 must satisfy) and can be written before sub-task 1's diff lands — TDD-friendly demo gate signal.

**Demo criteria** (PASS = ALL boxes checked):
- [ ] `pytest tests/test_services/test_whale_orchestrator_size_usd.py -x` exits 0 (all three tests green)
- [ ] Smoke import: `python -c "from app.services.whale_orchestrator import WhaleOrchestrator"` exits 0 with empty stdout (no ImportError, no warning on stderr)
- [ ] No regression on existing whale orchestrator tests: `pytest tests/test_services/test_whale_orchestrator.py` exits 0 (file present per `Glob` check), and any other `test_whale_*` files under `tests/test_services/` remain green
- [ ] Advisory: `git diff --stat app/services/whale_orchestrator.py` shows <30 LOC added/modified (the change is local to one method body — small). Overshoot is NOT auto-fail; documented in commit body if exceeded.

**Exit gate** (anchored to phases.md § "Demo gate semantics"):
Backend-only demo. PASS requires: (1) pytest exit 0 on the new test file (3 tests green); (2) smoke import for `WhaleOrchestrator`; (3) no regression on existing whale orchestrator tests; (4) advisory <30 LOC. No screenshot, no UI verification — this slice is a pure parsing-logic fix with observability confined to the parser's return value (anchor "Behavior change rule" row N4: "NESSUN nuovo metric o log richiesto, l'osservabilità è nel return value della funzione di parsing"). Slice 3 is the LAST slice — no Slice 4 → Slice 5 transition exists, so inter-slice gate evaluation is N/A (per /tracer-feature Step 1.5 explicit rule "Mai invocato dopo l'ultimo slice"). If the implementer returns DONE_WITH_CONCERNS, the user reviews concerns directly (no downstream gate to trigger).

**Rollback:** Single-commit slice. If demo gate fails after `/fix-until-green` budget (3 iterations) is exhausted, the implementer marks the slice FAILED and reports evidence (last test output, files touched, diff state) per anchor "Recovery budget" — NO auto-rollback. If the user authorizes rollback after reviewing the FAILED report, recovery is `git revert <slice_3_commit_sha>` of the single Slice 3 commit. No DB migration to undo, no config schema change, no static asset to delete. The existing whale trades persisted in SQLite remain valid (their `size_usd` was computed by the old buggy logic; revert restores the same logic — no data corruption either way). Forward-compat caveat: future trades parsed with corrected logic produce semantically different `size_usd` values vs pre-Slice-3 rows — this is the intended behavior change (fix the bug, accept the discontinuity). Working tree pollution risk (Risks row 3) means the implementer commits ONLY the two scoped files; `git revert` will not touch unrelated working-tree changes.

---

## Domain Analysis (cross-slice)

- **Backend (DI wiring):** touched in Slice 1 (N1), Slice 2 (N2)
- **Backend (parsing logic):** touched in Slice 3 (N4)
- **Tests:** Slice 1, Slice 2, Slice 3 (mirror convention)
- **Frontend / config:** untouched (OFF-LIMITS)

Slice 1 e Slice 2 toccano lo stesso file (`app/core/dependencies.py`) → ordering N1→N2 obbligatoria (vedi Inter-slice gate trigger b).
Slice 3 file scope disgiunto → parallelizzabile con Slice 1 (default).

## Open Tensions

- (Nessuna al Round 5. Audit trail: `.claude/plan-hardened/tracer-anchors/REJECTED-SUGGESTIONS.md` per le 2 issue Codex INVALID.)

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Slice N2 dual-order test rivela race condition non vista in design | M | H | Demo gate impone esplicitamente test su entrambi gli ordini; failure trigger inter-slice gate (c) |
| Slice N1 e N2 sequenziali → wall-clock raddoppiato vs parallel | L | L | N4 parallelo a N1 mitiga; accettato come trade-off per file overlap |
| Working tree pollution (logs/bot.log lock, file untracked Phase 13) interferisce con git ops | M | M | Recovery budget esplicito: NO auto-rollback. Implementer raccoglie evidence e si ferma. |
| Status flag DONE_WITH_CONCERNS dimenticato dall'implementer → gate skip erroneo | L | M | Anchor row "Inter-slice gate" referenzia esplicitamente CLAUDE.md global status protocol; agent dispatch payload include riferimento |

## Done Checklist

- [ ] All 3 slices DONE in phases.md
- [ ] Demo gate evidence per slice (test output + diff stat)
- [ ] `lessons.md` aggiornato con workflow lesson (Step 3.1)
- [ ] Vault note proposta via `obsidian-kg` (Step 3.2)
- [ ] Anchors preservati (no drift dal contratto Slice 0)
- [ ] No regressions sulla test suite globale (`pytest -x`)
