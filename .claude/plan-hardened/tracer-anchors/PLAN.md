# Architectural Anchors — Tracer-feature N1+N2+N4

## Obiettivo
Produrre la tabella di anchors immutabili Slice 0 che governera l'esecuzione di /tracer-feature sui tre fix bounded N1 (snapshot wiring in scheduler), N2 (TradeStore singleton) e N4 (whale size/USD detection), fissando file scope, gate, e regole di recovery prima che i Round successivi del plan-hardened possano alterarli.

## File impattati
- .claude/plan-hardened/tracer-anchors/PLAN.draft.md (Round 1)
- .claude/plan-hardened/tracer-anchors/PLAN.v2.md (NEW — questo file, post-Codex review)
- (output futuro: phases.md alla project root, scritto da /tracer-feature Step 0.3)

## Architectural Anchors (la tabella richiesta)

| Anchor | Valore | Rationale |
|--------|--------|-----------|
| Skill canonical paths | tracer-bullets, codex-delegation, caveman-commit, obsidian-kg @ ~/.claude/skills/<name>/SKILL.md | Required-by-spec da /tracer-feature Step 0.2: la tabella anchors DEVE fissare le skill canoniche. Lock dei path evita drift se il tool inventory cambia tra Round; chi esegue legge SOLO da questi path, non da copie locali o citazioni inline. tracer-bullets governa slice semantics, codex-delegation la delega monitorata, caveman-commit il commit format, obsidian-kg la persistenza lessons. |
| Command paths | /tracer-feature, /plan-hardened, /refactor, /fix-until-green @ ~/.claude/commands/<name>.md | Required-by-spec da /tracer-feature Step 0.2: la tabella anchors DEVE fissare i command path. /tracer-feature e' driver primario, /plan-hardened gate inter-slice anchor-driven, /refactor discriminator semantico (NON usabile qui — i tre fix introducono behavior change), /fix-until-green recovery loop. Path globali, non project-local, perche' nessuno dei comandi e' shadowato in .claude/. |
| File scope per slice | Slice N1 = app/core/dependencies.py + tests/test_core/test_dependencies_*.py (NEW, mirror convention); Slice N2 = app/core/dependencies.py + tests/test_core/test_dependencies_*.py (stesso glob mirror, NO test edits altrove); Slice N4 = app/services/whale_orchestrator.py + tests/test_services/test_whale_orchestrator_*.py (mirror convention) | File disgiunti tra N4 e N1/N2 → N4 e' parallelizzabile con N1 o N2 (vedi Single-slice-per-blocker per regola di ordering). N1 e N2 condividono dependencies.py → strettamente sequenziali tra loro (no merge race). Tutte e tre le slice usano lo stesso glob mirror tests/test_<dir>/test_<module>_*.py — nessun test edit fuori da questo glob, coerentemente con OFF-LIMITS. Nessuna slice tocca app/main.py: il lifespan e' orchestratore, non implementer, e modificarlo violerebbe il bounded scope dei blocker. |
| OFF-LIMITS | app/main.py (lifespan e' orchestratore — chiama get_execution_engine, non lo costruisce); config/* (tunables anchor-invariant per il tracer); tests/ outside mirroring convention (ANCHE per N2: solo tests/test_core/test_dependencies_*.py); qualunque modulo app/ non listato nel file scope (intelligence_orchestrator, trade_store internals, snapshot_writer signature) | Tracer != refactor: behavior change e' confinata ai punti di wiring e parsing identificati nei bounded blocker. Allargare lo scope a config/main/orchestrator interni invalida l'invariante "demoable end-to-end senza side-effect cross-cutting" e introduce regression risk fuori dai test mirroring. |
| Inner loop | Step 1.2 inline single-shot (ogni slice = 1 file principale + 1 test file → no wave-orchestrator necessario; intra-slice parallelism solo se ≥2 sub-task disjoint emergono in fase di implementazione) | Overhead di wave-orchestrator (heartbeat 2-5min, status protocol DONE/BLOCKED/NEEDS_CONTEXT, restart logic) non giustificato per fix <30 LOC su singolo file. Single-shot inline mantiene context locality, riduce token cost, e preserva la possibilita' di un /fix-until-green budget senza pagare cold-start su ogni iter. |
| Inter-slice gate | plan-hardened anchor-driven. Trigger CONCRETI (gate INVOKE se almeno uno vero): (a) implementer ritorna status DONE_WITH_CONCERNS (status protocol del CLAUDE.md globale: DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT — settato dall'agente che chiude la slice); (b) transizione N1→N2 (entrambe modificano dependencies.py — gate previene merge conflict semantico tra wiring snapshot e singleton store); (c) qualsiasi slice fallisce demo gate al primo run. Trigger NON attivo per: transizioni che coinvolgono N4 (file disgiunto), slice DONE pulita su file disgiunti. | Tre slice piccole e bounded non richiedono gate critico per default — il costo (5-round pipeline Sonnet/Codex/Opus) supera il beneficio. Trigger condizionali concreti (status flag esplicito + condizione di file-overlap + demo failure) sostituiscono il vago "anchor file dependencies.py" e definiscono univocamente quando il gate scatta. Lo status flag e' settato dall'implementer subagent, non inferito. |
| Demo gate semantics | Backend-only (no UI). Per slice (PASS = TUTTI veri): (1) pytest exit 0 sui test del modulo target; (2) per Slice N2 specificamente: i test devono coprire ENTRAMBI gli ordini di inizializzazione (scheduler-prima-di-engine E engine-prima-di-scheduler) e asserire singleton identity (`get_trade_store()` ritorna la stessa istanza usata da get_execution_engine e da start_intelligence_scheduler — `assert a is b is c`); (3) smoke import test stdout (`python -c "import app.<module>"`); (4) git diff size: advisory check, atteso <30 LOC escluso test code — un superamento NON e' fail automatico, e' segnale per re-leggere lo scope (l'implementer documenta il motivo del superamento nel commit body). Nessuno screenshot. | Tre fix sono backend (DI wiring, parsing). UI dashboard non e' impattata. La tightening su Slice N2 e' necessaria perche' uno smoke import puo' passare mentre la lazy-store race rimane: il dual-order test + singleton-identity assertion e' il solo modo di provare l'invariante che N2 deve garantire. La LOC threshold e' demota da gate a advisory check perche' il payload bounded non autorizza a respingere un fix corretto per superamento di una soglia arbitraria; resta utile come scope-discipline signal. |
| Behavior change rule | Tracer = nuova behavior osservabile. N1: snapshot writer wired anche in monitoring-only e' osservabile via filesystem (file snapshot scritto). N2: TradeStore singleton accessibile pre-engine e' osservabile via DI introspection (singleton identity test). N4: whale size/USD corretto e' osservabile via test diretto sul valore di output del parser (es. `parsed.usd == 500` per 10000 share × $0.05) — NESSUN nuovo metric o log richiesto, l'osservabilita' e' nel return value della funzione di parsing. NON refactor (zero-behavior-change e' /refactor). | Distinzione lean: i tre fix introducono behavior visibile dall'esterno. L'osservabilita' di N4 e' confinata al return value testato — il payload non chiede nuove metriche o log entries, e aggiungerle sarebbe scope creep. Cio' qualifica /tracer-feature come tool corretto vs /refactor (che fallirebbe il vincolo zero-behavior-change). |
| Recovery budget | /fix-until-green budget 3 iter dentro slice se test red. Oltre → slice marked FAILED, l'implementer riporta evidenze (last error, files touched, diff state) e si ferma. NESSUN auto-rollback git, NESSUNA escalation automatica a planning-specialist: la decisione di rollback/re-scope/escalation e' dell'utente sulla base del FAILED report. Lo stato locale (modifiche non committate) resta intatto per ispezione. | 3 iter e' soglia empirica per discriminare "errore meccanico recuperabile" (typo, import mancante, assertion off-by-one) da "modello mentale errato" (assunzione su DI lifecycle, race condition non vista). Oltre 3 iter, persistere brucia token senza convergere — meglio fermarsi e riportare. Auto-rollback/escalation rimossi perche' non autorizzati dal payload bounded e perche' un rollback meccanico potrebbe scartare lavoro locale non correlato (working tree del progetto contiene altre modifiche pendenti). |
| Single-slice-per-blocker | Accettata: N1 = Slice 1, N2 = Slice 2, N4 = Slice 3 in default. Regola di ordering UNIVOCA: N1 → N2 sequenziali (entrambe su dependencies.py, no merge race tollerata); N4 PUO' essere eseguita in parallelo con N1 o N2 sotto la separazione di file scope gia' fissata (whale_orchestrator.py disgiunto da dependencies.py), oppure ultima — la scelta default e' parallela con N1 per ridurre wall-clock; "ultima per safety" NON e' la regola, e' un fallback solo se l'utente esplicitamente richiede esecuzione strettamente seriale. | Coerente con principio Pocock "demoable end-to-end": ogni slice deve essere un cambiamento osservabile minimo ma completo. Decomporre N1 in "sposta wiring" + "test wiring" produce slice non-demoable. La convention "1 blocker = 1 slice" mantiene coherent unit of demo. L'ordering ambiguo del Round 1 ("parallelizzabile o ultimo") e' risolto: parallelism N4-con-N1 e' la regola di default sotto la separazione di file scope; la modalita' seriale e' opt-in, non implicita. |

## Ambiguita' note (CAMPO OBBLIGATORIO)
Nessuna. Il payload del Round 1 fissa esplicitamente: (a) i tre blocker con line numbers e difetto descritto, (b) il file scope per slice, (c) il branch target, (d) la convention di test mirroring. Il Round 2 Codex ha sollevato 9 issue: 2 rejected come INVALID (AR-001, AR-002 — required-by-spec da /tracer-feature Step 0.2), 7 valid e mergeate nei rationale qui sopra (AR-003 file-scope tightening N2, AR-004 demo gate singleton-identity, AR-005 LOC advisory non gate, AR-006 inter-slice trigger concreto, AR-007 N4 osservabilita' senza metric/log, AR-008 no auto-rollback, AR-009 N4 ordering univoco).

## Assunzioni fatte (CAMPO OBBLIGATORIO)
- I test della Slice N1 verificano ANCHE la costruzione standalone di SnapshotWriter senza ExecutionEngine costruito (caso monitoring-only mode, bot.auto_start=false), non solo il path nominal con engine costruito.
- Il fix N2 preserva backward compatibility sul wiring path attuale di get_execution_engine: se get_execution_engine viene invocato prima di start_intelligence_scheduler, deve continuare a funzionare e a popolare il TradeStore singleton (non duplicare istanza). I test N2 coprono ESPLICITAMENTE entrambi gli ordini di inizializzazione (vedi Demo gate semantics).
- Il fix N4 non invalida i whale trade gia' persistiti nello store o nel KG: la logica di parsing corretta si applica solo agli eventi futuri. Nessuna migration o backfill richiesto. L'osservabilita' del fix e' nel return value del parser, non in nuovi metric/log.
- La branch phase-13-master-sync e' la base di lavoro corretta; nessun rebase su master e' richiesto durante il tracer (rebase e' decisione di /plan-hardened o del flusso PR esterno).
- "Bot.auto_start=false in app/main.py:43-51" si riferisce alla configurazione runtime che istanzia FastAPI lifespan SENZA invocare get_execution_engine; il fix N1 deve quindi spostare il wiring a un hook (start_intelligence_scheduler) che viene invocato in entrambi i mode (monitoring-only e full-bot).
- Il payload menziona "Phase 13 Fix 4 stated goal" — si assume che il goal sia documentato altrove (todo.md o commit message di Phase 13) e che il tracer non debba ridiscuterlo, solo realizzarlo.
- Tests N4 verificano sia il caso edge (10000 share × $0.05 = $500, non $10000) sia il caso storico di non-regressione (parsing di trade gia' working).
- Lo status protocol DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT e' quello canonico del CLAUDE.md globale (sezione "Implementer status protocol"); l'implementer subagent imposta lo stato esplicitamente nel report finale di slice. Il gate inter-slice legge questo flag, non lo deduce.

## Verifica (comandi eseguibili)
- `pytest tests/test_core/test_dependencies_*.py -x` (dopo Slice N1+N2 — copre entrambe sotto lo stesso glob mirror)
- `pytest tests/test_services/test_whale_orchestrator_*.py -x` (dopo Slice N4)
- `python -c "from app.core.dependencies import get_trade_store; print('OK')"` (smoke N2)
- `git diff --stat HEAD~1..HEAD` (advisory scope check — segnale, non gate; superamento richiede motivazione nel commit body)
- Per N2 specificamente: il test deve includere assertion `get_trade_store() is engine.trade_store is scheduler.trade_store` su ENTRAMBI gli ordini di init (scheduler-first e engine-first)
